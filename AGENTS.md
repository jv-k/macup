# Agent instructions

Read `CLAUDE.md` in this directory first. It holds the stack, the source-of-truth pointers, the build loop, and the don't list for this repo.

This applies to every coding agent: Gemini, Codex, Copilot, and any other. `CLAUDE.md` is the single entry point, kept short and current on purpose. Start there.
