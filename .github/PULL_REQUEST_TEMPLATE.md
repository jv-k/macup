<!-- Use Conventional Commits for the title and every commit: type(scope): summary -->

## What changed and why

<!-- Lead with the change and the reason. Link the issue it implements. -->

## Checklist

- [ ] Title and commits are Conventional Commits (`type(scope): summary`).
- [ ] Lint and types pass (`pnpm lint && pnpm typecheck`).
- [ ] Tests pass (`pnpm test`), and new behaviour has a test.
- [ ] Build passes (`pnpm build`).
