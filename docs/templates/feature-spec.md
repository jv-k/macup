# <Feature>: spec

## What it does

One paragraph a non-engineer understands.

## User-facing behaviour

The states and flows: empty, loading, error, and the success path.

## Design brief

The product's design language: the visual intent the design generation builds from, on
top of the states in User-facing behaviour and the data shapes in Contract (frames are
drawn against those real values, not lorem). See
[../conventions/figma-pipeline.md](../conventions/figma-pipeline.md).

- Precedent: <a product or two whose feel is close, and what to take from each>.
- Brand and tone: <flat or soft, editorial or product, accent-led or restrained>.
- Layout and density: <grid, default density, where it goes compact>.
- Token seed: <palette, type scale, radius, spacing intent>.
- Figma: <link once the file exists>.

## Contract

The data it reads and writes, the schema, and the external dependencies. Link the source
of truth for each.

## Acceptance

Done when ... (each line a testable condition).

## Out of scope

What this explicitly does not do.
